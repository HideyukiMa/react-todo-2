import React, { FC } from 'react';
import Title from '../../components/title';

const TodoListUI: FC = () => {
	return (
		<div>
			<Title title={'TODO一覧'} />
			TodoListUI
		</div>
	);
};

export default TodoListUI;
